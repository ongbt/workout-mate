# Cloudflare Pages configuration for workout-mate
# Env vars are managed via Cloudflare API (see .env.staging for staging URLs)
# Staging branch → preview deployment → resolute-wildcat-738 (dev Convex)
# Main branch → production deployment → rapid-gull-240 (prod Convex)

[pages]
build_dir = "dist"
build_command = "pnpm build"
