# Workout Mate — Project CLAUDE.md

## Stack

- **Frontend**: React 19 + Vite + TypeScript
- **Backend**: Convex (`ongbt` team, `workout-mate` project)
- **Package manager**: pnpm

## Convex deployments

- **Dev**: `dev:resolute-wildcat-738`
- **Prod**: `prod:rapid-gull-240`

Use `--env-file .env.production` when deploying to prod. Env files:

- `.env.local` — dev (default)
- `.env.staging` — staging
- `.env.production` — production
